<%= render 'settings' %>

<div class="row translations">
  <div class="col-md-4">
    <%= render 'fields' %>
  </div>
  <div class="col-md-8">
    <%= form_for [:admin, @resource] do |f| %>
      <%= render 'form_fields_view', f: f %>
    <% end %>
  </div>
</div>
